apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: clan-gateway
  labels:
    app: gateway-service
spec:
  selector:
    app: gateway-service
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
